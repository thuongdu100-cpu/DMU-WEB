# Khi chỉ chạy `npm run dev` trong thư mục frontend: proxy /api → API busboy (mặc định 3001).
# Trùng với `npm run dev` từ thư mục gốc (cross-env VITE_API_TARGET).
VITE_API_TARGET=http://localhost:3001
