# Vite proxy /api, /uploads, /posts → máy chạy API (mặc định cổng 3001).
# Không cần VITE_API_TARGET nếu API chạy trên cùng PC: vite.config.js tự chọn IPv4 LAN hoặc 127.0.0.1.
# Ghi đè khi cần, ví dụ API trên máy khác:
# VITE_API_TARGET=http://192.168.1.10:3001
